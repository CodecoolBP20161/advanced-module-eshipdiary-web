language: java
install: ./mvnw clean test -Dmaven.javadoc.skip=true -Dspring.profiles.active=travis -B -V
sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer
jdk:
  - oraclejdk8
notifications:
  email: false

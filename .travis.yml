language: java
install: mvn clean test -Dmaven.javadoc.skip=true -Dspring.profiles.active=travis -B -V
sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer
jdk:
  - oraclejdk8
notifications:
  email: false
before_install:
  - chmod +x mvnw